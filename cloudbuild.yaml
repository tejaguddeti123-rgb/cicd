steps:
- name: 'gcr.io/cloud-builders/docker'
  args: ['build', '-t', 'IMAGE_NAME', '.']

options:
  default_logs_bucket_behavior: REGIONAL_USER_OWNED_BUCKET

serviceAccount: your-sa@project.iam.gserviceaccount.com
